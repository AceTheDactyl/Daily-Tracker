# Google Calendar API Configuration
# Follow these steps to get your credentials:
#
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Calendar API:
#    - Go to "APIs & Services" > "Library"
#    - Search for "Google Calendar API"
#    - Click "Enable"
#
# 4. Create OAuth 2.0 Client ID:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "OAuth client ID"
#    - Application type: "Web application"
#    - Add authorized JavaScript origins:
#      - http://localhost:5173 (for local development)
#      - https://yourusername.github.io (for GitHub Pages)
#    - Copy the Client ID
#
# 5. Create API Key:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "API key"
#    - Restrict the key to "Google Calendar API" for security
#    - Copy the API Key
#
# 6. Copy this file to .env and fill in your credentials

# OAuth 2.0 Client ID (Web application)
VITE_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com

# API Key (restricted to Google Calendar API)
VITE_GOOGLE_API_KEY=your_api_key_here

# Calendar ID (usually 'primary' for personal calendar, or a specific calendar ID)
VITE_GOOGLE_CALENDAR_ID=primary
